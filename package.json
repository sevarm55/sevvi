{
  "name": "sevvi",
  "version": "1.0.0",
  "description": "Sevvi - custom terminal emulator",
  "main": "dist/main/main.js",
  "scripts": {
    "build:ts": "tsc",
    "dev": "tsc && electron .",
    "build": "tsc && electron-builder --mac",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@xterm/xterm": "^5.5.0",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0",
    "node-pty": "^1.0.0"
  }
}
